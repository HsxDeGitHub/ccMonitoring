## 背景
我在平时开发的过程中，启动了claude code，然后使用claude code在执行任务。这时候我回切到其他程序其他界面去做其他事情，这时候我又担心claude code在运行过程中停止了，或者需要向我确认请求，所以我需要时不时经常的切回claude code的界面去查看任务是否还在继续执行。

## 要求
我是mac系统，我希望能开发一个程序，能一直挂载在桌面，简洁的显示启动的claude code的运行状态，这样我在其他界面也能看到claude code的任务是否还在继续执行。